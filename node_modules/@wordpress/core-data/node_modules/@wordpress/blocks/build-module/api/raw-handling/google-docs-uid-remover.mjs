// packages/blocks/src/api/raw-handling/google-docs-uid-remover.ts
import { unwrap } from "@wordpress/dom";
function googleDocsUIdRemover(node) {
  const el = node;
  if (!el.id || el.id.indexOf("docs-internal-guid-") !== 0) {
    return;
  }
  if (el.tagName === "B") {
    unwrap(node);
  } else {
    el.removeAttribute("id");
  }
}
export {
  googleDocsUIdRemover as default
};
//# sourceMappingURL=google-docs-uid-remover.mjs.map
